# GitLab CI/CD Rules Documentation
# ==============================
#
# Rule Patterns and Their Meanings
# -----------------------------
#
# 1. Skip Main Branch Rule
# ---------------------
# if: $CI_COMMIT_BRANCH == "main"
# when: never
# Meaning: Skip the job when the pipeline runs on the main branch
#
# 2. Skip Version Branch Rule
# ------------------------
# if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
# when: never
# Meaning: Skip the job when the branch name ends with a version pattern
# Example matches: release-v1.0.0, feature-v2.3.4, hotfix-v10.2.1
#
# 3. Skip Merge Request Rule
# -----------------------
# if: $CI_MERGE_REQUEST_IID != null
# when: never
# Meaning: Skip the job when the pipeline runs in context of a merge request
#
# 4. Integration Test Execution Rules
# -------------------------------
# Location: '.gitlab/jobs/ci-job-definitions.yml'
#
# if: $EXECUTE_INTEGRATION_TEST == "Yes"
# when: always
# Meaning: Execute the job when the EXECUTE_INTEGRATION_TEST variable is set to "Yes"
#
# 5. Integration Branch Rules
# -----------------------
# Location: '.gitlab/jobs/ci-job-definitions.yml'
#
# if: $CI_COMMIT_BRANCH == "integ"
# when: always
# Meaning: Execute the job when the pipeline runs on the "integ" branch
#
# 6. Version Tag Rules
# -----------------
# if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
# when: always
# Meaning: Execute the job when the pipeline runs on a tag matching version pattern
# Example matches: v1.0.0, v2.3.4, v10.2.1
#
# 7. Project-Specific Version Tag Rules
# --------------------------------
# if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
# when: always
# Meaning: Execute the job only on specific project (21753) and when tag matches version pattern
#
# 8. Project-Specific Integration Branch Rules
# ---------------------------------------
# if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_BRANCH == "integ"
# when: always
# Meaning: Execute the job only on specific project (21753) and on the "integ" branch
#
# 9. Default Skip Rule
# -----------------
# when: never
# Meaning: Skip the job if none of the above conditions match
#
# Rule Keywords Explained
# --------------------
# - if: Condition that must be true for the rule to apply
# - when: Defines job behavior
#   * always: Run job when conditions match
#   * never: Skip job (prevent execution)
#   * manual: Require manual intervention to run
#
# Variables Used in Rules
# --------------------
# CI_MERGE_REQUEST_IID:
#   - ID of the merge request if pipeline runs in MR context
#   - null when pipeline runs in other contexts
#
# CI_PROJECT_ID:
#   - GitLab's internal project identifier
#   - "21753" represents specific project
#
# CI_COMMIT_TAG:
#   - The tag name for which the pipeline is running
#   - Pattern v(\d.+){2}\d matches semantic versioning
#
# CI_COMMIT_BRANCH:
#   - The branch name for which the pipeline is running
#   - Special values: "main", "integ"
#   - Can match version pattern for release branches
#
# EXECUTE_INTEGRATION_TEST:
#   - Custom variable to control integration test execution
#   - "Yes" enables integration tests
#
# Regular Expression Patterns
# ------------------------
# 1. Version Tag Pattern: v(\d.+){2}\d
#    - v: Literal 'v' at start
#    - (\d.+){2}: Two groups of digits followed by any characters
#    - \d: Final digit
#    - Examples: v1.0.0, v2.3.4, v10.2.1
#
# 2. Version Branch Pattern: .*v(\d.+){2}\d$
#    - .*: Any characters at start
#    - v(\d.+){2}\d: Same version pattern as above
#    - $: End of string
#    - Examples: release-v1.0.0, feature-v2.3.4

image: public.ecr.aws/docker/library/node:22.15.0

before_script:
  - rm -rf /var/lib/apt/lists/*
  - apt-get update
  - apt-cache gencaches
  - apt-get install -y zip
  - apt-get install -y jq

include:
  # - '.gitlab/jobs/ci-job-definitions.yml'
  # Module integration test jobs
  - '.gitlab/jobs/security/integ-test.yaml'
  # Mirror jobs for CodeCommit replication
  - '.gitlab/jobs/mirror-job.yaml'
  - '.gitlab/jobs/mirror-universal-config-repo.yaml'

stages:
  - pipeline
  - mirror
  - security

variables:
  KUBERNETES_HELPER_MEMORY_REQUEST: "1Gi"
  KUBERNETES_HELPER_MEMORY_LIMIT: "2Gi"

accelerator:scan-sensitive-content:
  stage: pipeline
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "1"
    KUBERNETES_CPU_LIMIT: "1"
    KUBERNETES_MEMORY_REQUEST: "2Gi"
    KUBERNETES_MEMORY_LIMIT: "2Gi"
    # SENSITIVE_DOMAINS only needed in main repository
    # Forks will use basic pattern scanning automatically
    SCAN_EXCLUDE_DIRS: ".git node_modules build dist coverage"
    SCAN_EXCLUDE_FILES: "*.log *.zip *.tar.gz *.tgz"
  script:
    - echo "Running security scan for sensitive URLs and content..."
    - echo "Sensitive domains configured:" ${SENSITIVE_DOMAINS:-"(not set - will fail)"}
    - chmod +x .gitlab/scripts/security-scan.sh
    - ./.gitlab/scripts/security-scan.sh
  rules:
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always

accelerator:daily-vulnerability-scan:
  stage: security
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  script:
    - chmod +x .gitlab/scripts/vulnerability-scan.sh
    - chmod +x .gitlab/scripts/issue-manager.sh
    - ./.gitlab/scripts/vulnerability-scan.sh
    - ./.gitlab/scripts/issue-manager.sh
  artifacts:
    name: vulnerability-scan-results
    when: always
    paths:
      - software-dependency-findings.json
      - filtered-findings.json
      - masked-findings.json
    expire_in: 30 days
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "Daily Vulnerability Scan" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_PIPELINE_SOURCE == "web"
      when: manual
    - when: never

accelerator:cve-remediation:
  stage: security
  needs: []
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
    VULN_ASSIGNEE: "erwaxler"
  tags:
    - arch:amd64
  before_script:
    - apt-get update && apt-get install -y jq
    - git config user.email "lza-cve-bot@amazon.com"
    - git config user.name "LZA CVE Remediation Bot"
    - git remote set-url origin "https://oauth2:${VULN_SCAN_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
    - echo "Tools:" && yarn --version && jq --version && git --version
  script:
    - chmod +x .gitlab/scripts/cve-remediate.sh
    - ./.gitlab/scripts/cve-remediate.sh
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "Daily Vulnerability Scan" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_PIPELINE_SOURCE == "web"
      when: manual
    - when: never
  artifacts:
    paths:
      - cve-remediation-report.json
    expire_in: 30 days
    when: always

accelerator:nightly-github-sync:
  stage: security
  variables:
    KUBERNETES_CPU_REQUEST: "1"
    KUBERNETES_CPU_LIMIT: "1"
    KUBERNETES_MEMORY_REQUEST: "2Gi"
    KUBERNETES_MEMORY_LIMIT: "2Gi"
  tags:
    - arch:amd64
  allow_failure: true
  script:
    - chmod +x .gitlab/scripts/github-issue-sync.sh
    - ./.gitlab/scripts/github-issue-sync.sh
  artifacts:
    name: github-sync-results
    when: always
    paths:
      - github-sync-summary.json
      - failed-items.json
    expire_in: 30 days
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "Nightly GitHub Sync" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_PIPELINE_SOURCE == "web" && $CI_COMMIT_BRANCH == "integ" && $GITHUB_SYNC_ALLOW_MANUAL == "true"
      when: manual
    - when: never

accelerator:build:
  stage: pipeline
  variables:
    KUBERNETES_CPU_REQUEST: "16"
    KUBERNETES_CPU_LIMIT: "16"
    KUBERNETES_MEMORY_REQUEST: "64Gi"
    KUBERNETES_MEMORY_LIMIT: "64Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=49152"
    - "npm install yarn@1.22.22"
    - "cd source"
    - "yarn install"
    - "yarn build"
  artifacts:
    name: solution-build
    untracked: true
    when: on_success
    paths:
      - source
    expire_in: 3 hrs
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
accelerator:lint:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "8"
    KUBERNETES_CPU_LIMIT: "8"
    KUBERNETES_MEMORY_REQUEST: "32Gi"
    KUBERNETES_MEMORY_LIMIT: "32Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=28672"
    - "npm install yarn@1.22.22"
    - "cd source"
    - "yarn install"
    - "yarn lint"
    - "yarn lerna run precommit --stream"
    - "export ACCELERATOR_PREFIX=AWSAccelerator"
    # - "yarn validate-config packages/@aws-accelerator/accelerator/test/configs/all-enabled/" # Validate all enabled config files
    # - "yarn validate-config packages/@aws-accelerator/accelerator/test/configs/no-org-config/" # Validate no-org config files"
    - "yarn git-clean"
    - "yarn run viper-scan"
    # Check if build  has changed any of the
    - git add -A
    - |
      if [[ -n $(git status --porcelain | grep -E 'source/packages/@aws-accelerator/config/lib/schemas/') ]]; then
        echo "Uncommitted changes or untracked files detected in schema folder listed below. Please run yarn build-clean locally, then commit and push the updated schema file to resolve."
        git status --porcelain | grep -E 'source/packages/@aws-accelerator/config/lib/schemas/'
        exit 1
      else
        echo "No changes or untracked files detected in schema folder"
      fi
    - git reset
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: never
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
accelerator:attribution-check:
  stage: pipeline
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - echo "This merge request contains commits from an external author and is incorrectly configured to squash commits, which can erase author attribution. Please set \"Squash commits on merge\" to false and retry the pipeline."
    - exit 1
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_AUTHOR !~ /@amazon/ && $CI_MERGE_REQUEST_SQUASH_ON_MERGE == "true"
      when: always
    - when: never

release:test:utils:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "16"
    KUBERNETES_CPU_LIMIT: "16"
    KUBERNETES_MEMORY_REQUEST: "64Gi"
    KUBERNETES_MEMORY_LIMIT: "64Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=49152"
    - "cd source/packages/@aws-accelerator/utils"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/utils/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
release:test:cdk-extensions:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "16"
    KUBERNETES_CPU_LIMIT: "16"
    KUBERNETES_MEMORY_REQUEST: "64Gi"
    KUBERNETES_MEMORY_LIMIT: "64Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=49152"
    - "cd source/packages/@aws-cdk-extensions/cdk-extensions"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-cdk-extensions/cdk-extensions/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
unit-test:accelerator:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "16"
    KUBERNETES_CPU_LIMIT: "16"
    KUBERNETES_MEMORY_REQUEST: "64Gi"
    KUBERNETES_MEMORY_LIMIT: "64Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=49152"
    - "cd source/packages/@aws-accelerator/accelerator"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/accelerator/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:config:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/config"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/config/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:constructs:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "16"
    KUBERNETES_CPU_LIMIT: "16"
    KUBERNETES_MEMORY_REQUEST: "64Gi"
    KUBERNETES_MEMORY_LIMIT: "64Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=49152"
    - "cd source/packages/@aws-accelerator/constructs"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/constructs/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:govcloud-account-vending:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/govcloud-account-vending"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/govcloud-account-vending/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:installer:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/installer"
    - "yarn test:unit"
    - "yarn cdk synth"
    - "cp cdk.out/AWSAccelerator-InstallerStack.template.json $CI_PROJECT_DIR/AWSAccelerator-InstallerStack.template.json"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/installer/test-reports/*.xml
    paths:
      - AWSAccelerator-InstallerStack.template.json
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:modules:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/modules"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/modules/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:tools:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/tools"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/tools/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:aws-lza:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-lza"
    - "yarn test:unit"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-lza/test-reports/*.xml
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
unit-test:installer-container:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "8Gi"
    KUBERNETES_MEMORY_LIMIT: "8Gi"
  tags:
    - arch:amd64
  script:
    - "export NODE_OPTIONS=--max_old_space_size=6144"
    - "cd source/packages/@aws-accelerator/installer-container"
    - "yarn test:unit"
    - "yarn cdk synth --context use-external-pipeline-account=true"
    - "cp cdk.out/AWSAccelerator-InstallerContainerStack.template.json $CI_PROJECT_DIR/AWSAccelerator-InstallerContainerStack.template.json"
  artifacts:
    when: always
    reports:
      junit:
        - source/packages/@aws-accelerator/installer-container/test-reports/*.xml
    paths:
      - AWSAccelerator-InstallerContainerStack.template.json
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: never
    - if: $CI_COMMIT_BRANCH =~ /.*v(\d.+){2}\d$/
      when: never
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - when: always
installer:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  script:
    - "cd source"
    - "cd packages/@aws-accelerator/installer"
    - "yarn cdk synth"
    - "cp cdk.out/AWSAccelerator-InstallerStack.template.json $CI_PROJECT_DIR/AWSAccelerator-InstallerStack.template.json"
  artifacts:
    name: "installer-template"
    when: on_success
    paths:
      - AWSAccelerator-InstallerStack.template.json
  rules:
    - if: $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
installer-container:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  script:
    - "cd source/packages/@aws-accelerator/installer-container"
    - "yarn cdk synth --context use-external-pipeline-account=true"
    - "cp cdk.out/AWSAccelerator-InstallerContainerStack.template.json $CI_PROJECT_DIR/AWSAccelerator-InstallerContainerStack.template.json"
  artifacts:
    name: "installer-template"
    when: on_success
    paths:
      - AWSAccelerator-InstallerContainerStack.template.json
  rules:
    - if: $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
workshop:
  stage: pipeline
  needs: ["accelerator:build"]
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  when: manual
  script:
    - "cd source"
    - "export version=$(cat package.json | jq -r '.version')"
    - "cd packages/@aws-accelerator/installer"
    - "yarn run cdk synth -c enable-single-account-mode=true --output $CI_PROJECT_DIR"
    - "cd ../../../../deployment"
    - "chmod +x ./build-open-source-dist.sh && ./build-open-source-dist.sh lza-on-aws-workshop"
    - "cp ./open-source/lza-on-aws-workshop.zip $CI_PROJECT_DIR/lza-on-aws-workshop.zip"
  artifacts:
    name: "lza-on-aws-workshop"
    when: on_success
    paths:
      - lza-on-aws-workshop.zip
      - AWSAccelerator-InstallerStack-Workshop.template.json
  rules:
    - if: $CI_COMMIT_BRANCH == "integ"
      when: always
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
release:build-typedocs:
  stage: pipeline
  needs:
    - job: accelerator:build
      artifacts: true
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "8"
    KUBERNETES_CPU_LIMIT: "8"
    KUBERNETES_MEMORY_REQUEST: "32Gi"
    KUBERNETES_MEMORY_LIMIT: "32Gi"
    AWS_DEFAULT_REGION: us-east-1
    AWS_CREDS_TARGET_ROLE: "${GITLAB_S3_ROLE}"
    BUILD_S3_PATH: s3://${BUCKET_NAME}/build-release/typedocs.zip
    VERSIONED_S3_PATH: s3://${BUCKET_NAME}/${CI_COMMIT_TAG}/typedocs.zip
    SITE_URL: "${GITLAB_SITE_URL}"
  before_script:
    # Install dependencies
    - "rm -rf /var/lib/apt/lists/*"
    - "apt-get update"
    - "apt-cache gencaches"
    - "apt-get install -y zip"
    - "wget --progress=dot:mega -O /tmp/awscliv2.zip https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
    - "unzip -q /tmp/awscliv2.zip -d /tmp"
    - "/tmp/aws/install"
  script:
    # Generate docs
    - "cd source"
    - "mkdir -p mkdocs/docs/typedocs"
    - "yarn docs"
    # Move to generated typedocs dir
    - "cd mkdocs/docs/typedocs"
    # Archive and upload to S3
    - "zip -rqy typedocs.zip *"
    - "aws s3 cp ./typedocs.zip ${VERSIONED_S3_PATH}"
    - "aws s3 cp ./typedocs.zip ${BUILD_S3_PATH}"
    - "cp ./typedocs.zip ${CI_PROJECT_DIR}/typedocs.zip"
  artifacts:
    name: TypeDocs
    when: on_success
    paths:
      - typedocs.zip
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

# =============================================================================
# Container build templates (shared by release and integ jobs)
# =============================================================================

.artifact-al2023-prep-template:
  stage: pipeline
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "8"
    KUBERNETES_CPU_LIMIT: "8"
    KUBERNETES_MEMORY_REQUEST: "32Gi"
    KUBERNETES_MEMORY_LIMIT: "32Gi"
    VERSION: $CI_COMMIT_SHORT_SHA
  before_script:
    - dnf install nodejs22 tar gzip findutils -y
    - npm install -g yarn
  image:
    name: public.ecr.aws/amazonlinux/amazonlinux:latest
    entrypoint:
      - ""
  script:
    - "export NODE_OPTIONS=--max_old_space_size=28672"
    - "cd $CI_PROJECT_DIR/deployment && ls -lart . && $CI_PROJECT_DIR/deployment/build-s3-dist.sh lza $VERSION"
  artifacts:
    name: al2023_Prepare
    when: on_success
    untracked: true
    paths:
      - $CI_PROJECT_DIR/deployment/ecr/

.artifact-al2023-build-template:
  stage: pipeline
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "8"
    KUBERNETES_CPU_LIMIT: "8"
    KUBERNETES_MEMORY_REQUEST: "32Gi"
    KUBERNETES_MEMORY_LIMIT: "32Gi"
    VERSION: $CI_COMMIT_SHORT_SHA
  before_script:
    - mkdir -p /kaniko/.docker
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [""]
  script:
    - /kaniko/executor --context $CI_PROJECT_DIR/deployment/ecr/landing-zone-accelerator-on-aws/ --destination lza-$VERSION-al2023 --dockerfile $CI_PROJECT_DIR/deployment/ecr/landing-zone-accelerator-on-aws/Dockerfile --tarPath $CI_PROJECT_DIR/deployment/ecr/lza-$VERSION-al2023.tar.gz --no-push
  artifacts:
    name: al2023_build
    when: on_success
    paths:
      - $CI_PROJECT_DIR/deployment/ecr/lza-$VERSION-al2023.tar.gz

# =============================================================================
# Release container jobs (triggered by version tags)
# =============================================================================

# make a zip file upon commit tag
release:artifact-al2023-prep:
  extends: .artifact-al2023-prep-template
  variables:
    VERSION: $CI_COMMIT_TAG
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

# use created context to build
release:artifact-al2023-build:
  extends: .artifact-al2023-build-template
  needs: ["release:artifact-al2023-prep"]
  variables:
    VERSION: $CI_COMMIT_TAG
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

# =============================================================================
# Integ container jobs (triggered by integ branch post-merge)
# =============================================================================

integ:artifact-al2023-prep:
  extends: .artifact-al2023-prep-template
  rules:
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - when: never

integ:artifact-al2023-build:
  extends: .artifact-al2023-build-template
  needs: ["integ:artifact-al2023-prep"]
  rules:
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - when: never

integ:artifact-al2023-upload:
  stage: pipeline
  needs: ["integ:artifact-al2023-build"]
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
    AWS_DEFAULT_REGION: us-west-2
    AWS_CREDS_TARGET_ROLE: "${LZA_GITFARM_ROLE}"
  script:
    - "wget --progress=dot:mega -O /tmp/awscliv2.zip https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
    - "unzip -q /tmp/awscliv2.zip -d /tmp"
    - "/tmp/aws/install"
    - cd $CI_PROJECT_DIR/deployment/ecr
    - TARBALL="lza-${CI_COMMIT_SHORT_SHA}-al2023.tar.gz"
    - 'test -f "$TARBALL" || { echo "ERROR: $TARBALL not found"; exit 1; }'
    - echo "Found tarball $TARBALL"
    - echo "${CI_COMMIT_SHA}" > commit-info.txt
    - zip artifact.zip "$TARBALL" commit-info.txt
    - aws s3 cp artifact.zip "s3://${LZA_GITFARM_BUCKET}/lza-integ/artifact.zip"
    - echo "Upload complete. Commit SHA ${CI_COMMIT_SHA}"
  rules:
    - if: $CI_MERGE_REQUEST_IID != null
      when: never
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_BRANCH == "integ"
      when: always
    - when: never

# upload artifact into package for release
release:artifact-al2023-upload:
  stage: pipeline
  needs: ["release:artifact-al2023-build"]
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
    TOKEN:
  before_script:
    # ensure file exists before upload
    - ls -lart $CI_PROJECT_DIR/deployment/ecr/lza-$CI_COMMIT_TAG-al2023.tar.gz
  script:
    # uploads can only be to url/project/<project-id>/packages/generic/<package_name>/<version>/<package_file>
    # in local testing it was found that package_file must match.
    # For example, if the package file is named "test.tar.gz" then the upload url must be "url/project/<project-id>/packages/generic/<package_name>/<version>/test.tar.gz"
    - |
      curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" --upload-file $CI_PROJECT_DIR/deployment/ecr/lza-$CI_COMMIT_TAG-al2023.tar.gz "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/lza-container/${CI_COMMIT_TAG}/lza-${CI_COMMIT_TAG}-al2023.tar.gz"
    # this echo will help in release and also allow downloads from logs directly
    - echo "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/lza-container/${CI_COMMIT_TAG}/lza-${CI_COMMIT_TAG}-al2023.tar.gz"
    # get sha1sum and add that to a file
    - sha1sum $CI_PROJECT_DIR/deployment/ecr/lza-$CI_COMMIT_TAG-al2023.tar.gz >> $CI_PROJECT_DIR/lza-$CI_COMMIT_TAG-al2023.tar.gz.sha1sum
    - |
      curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" --upload-file $CI_PROJECT_DIR/lza-$CI_COMMIT_TAG-al2023.tar.gz.sha1sum "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/lza-container/${CI_COMMIT_TAG}/lza-${CI_COMMIT_TAG}-al2023.tar.gz.sha1sum"
  rules:
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

# release based on commit tag
release:artifact-al2023-release:
  stage: pipeline
  needs: ["release:artifact-al2023-upload"]
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  before_script:
    # this script ensures before_script which is common to test/build will not run for this specific container
    - echo "Releasing $CI_COMMIT_TAG"
  script:
    - |
      release-cli create --name "$CI_COMMIT_TAG" --tag-name $CI_COMMIT_TAG \
        --assets-link "{\"name\":\"lza-${CI_COMMIT_TAG}-al2023.tar.gz\",\"url\":\"${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/lza-container/${CI_COMMIT_TAG}/lza-${CI_COMMIT_TAG}-al2023.tar.gz\"}" \
        --assets-link "{\"name\":\"lza-${CI_COMMIT_TAG}-al2023.tar.gz.sha1sum\",\"url\":\"${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/lza-container/${CI_COMMIT_TAG}/lza-${CI_COMMIT_TAG}-al2023.tar.gz.sha1sum\"}" \
        --description "Landing Zone Accelerator on AWS container version $CI_COMMIT_TAG built on AL2023"
  rules:
    - if: $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

release:build-distributions:
  stage: pipeline
  needs:
    - job: accelerator:build
      artifacts: true
    - job: release:test:utils
      artifacts: false
    - job: unit-test:config
      artifacts: false
    - job: release:test:cdk-extensions
      artifacts: false
    - job: unit-test:constructs
      artifacts: false
    - job: unit-test:tools
      artifacts: false
    - job: unit-test:modules
      artifacts: false
    - job: unit-test:installer
      artifacts: false
    - job: unit-test:installer-container
      artifacts: false
    - job: unit-test:govcloud-account-vending
      artifacts: false
    - job: unit-test:accelerator
      artifacts: false
    - job: unit-test:aws-lza
      artifacts: false
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "8"
    KUBERNETES_CPU_LIMIT: "8"
    KUBERNETES_MEMORY_REQUEST: "32Gi"
    KUBERNETES_MEMORY_LIMIT: "32Gi"
    TEMPLATE_OUTPUT_BUCKET: "solutions-reference"
  before_script:
    - "apt-get update && apt-get install -y jq zip"
  script:
    - "cd source"
    - "export PUBLISHED_VERSION=$CI_COMMIT_TAG"
    - "cd ../deployment"
    - "source ./solution_config"
    - "./build-s3-dist.sh temp-bucket $PUBLISHED_VERSION"
    - "./build-open-source-dist.sh $SOLUTION_TRADEMARKEDNAME"
  artifacts:
    name: distributions
    when: on_success
    untracked: true
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

release:package-artifact:
  stage: pipeline
  needs:
    - job: release:build-distributions
      artifacts: true
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
  tags:
    - arch:amd64
  before_script:
    - "apt-get update && apt-get install -y zip"
  script:
    - "zip -r artifact.zip deployment/ source/ .gitignore README.md LICENSE.txt NOTICE.txt CODE_OF_CONDUCT.md CONTRIBUTING.md .github/ buildspec.yml CHANGELOG.md SECURITY.md .cfnnag_* .viperlightignore codescan-postbuild-custom.sh solution-manifest.yaml sonar-project.properties"
  artifacts:
    name: packaged-artifact
    when: on_success
    paths:
      - artifact.zip
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never

release:upload-to-cornerstone:
  stage: pipeline
  needs:
    - job: release:package-artifact
      artifacts: true
  tags:
    - arch:amd64
  variables:
    KUBERNETES_CPU_REQUEST: "4"
    KUBERNETES_CPU_LIMIT: "4"
    KUBERNETES_MEMORY_REQUEST: "16Gi"
    KUBERNETES_MEMORY_LIMIT: "16Gi"
    AWS_DEFAULT_REGION: us-east-1
    AWS_CREDS_TARGET_ROLE: "${CORNERSTONE_ROLE_ARN}"
    CORNERSTONE_PACKAGE_BUCKET: "${CORNERSTONE_PACKAGE_BUCKET}"
  before_script:
    - "apt-get update && apt-get install -y jq"
    - "wget --progress=dot:mega -O /tmp/awscliv2.zip https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
    - "unzip -q /tmp/awscliv2.zip -d /tmp"
    - "/tmp/aws/install"
  script:
    - "cd source"
    - "export PUBLISHED_VERSION=$CI_COMMIT_TAG"
    - "cd .."
    - 'export AUTHOR_EMAIL=$(echo "$CI_COMMIT_AUTHOR" | grep -oP ''(?<=<).*(?=>)'' || echo "unknown")'
    - aws s3 cp artifact.zip s3://$CORNERSTONE_PACKAGE_BUCKET/SO0199/$PUBLISHED_VERSION/$CI_COMMIT_SHA/artifact.zip --metadata "package=Landing-zone-accelerator-on-aws,commitId=$CI_COMMIT_SHA,solutionId=SO0199,solutionVersion=$PUBLISHED_VERSION,solutionName=landing-zone-accelerator-on-aws,commitauthor=$AUTHOR_EMAIL,commitmessage=$CI_COMMIT_MESSAGE,committimestamp=$CI_COMMIT_TIMESTAMP,enablequicksight=No,branch=$CI_COMMIT_MESSAGE"
    - 'echo "Published solution archive to s3://$CORNERSTONE_PACKAGE_BUCKET/SO0199/$PUBLISHED_VERSION/$CI_COMMIT_SHA/artifact.zip"'
  rules:
    - if: $CI_PROJECT_ID == "21753" && $CI_COMMIT_TAG =~ /v(\d.+){2}\d/
      when: always
    - when: never
